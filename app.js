const API="https://sports-ai-backend-1-qh65.onrender.com";let all=[];const esc=x=>String(x??"—").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));const val=(o,ks,d="—")=>{for(const k of ks)if(o&&o[k]!=null&&o[k]!=="")return o[k];return d};
function objName(v){if(v==null)return null;if(typeof v==="string")return v;if(typeof v==="object")return v.Name||v.name||v.TeamName||v.teamName||v.Abbreviation||v.abbreviation||v.DisplayName||v.displayName||v.ShortName||v.shortName||v.Code||v.code||null;return null}
function team(p,side){let keys=side==="home"?["homeTeam","HomeTeam","home_team","Home","home","homeTeamName","HomeTeamName"]:["awayTeam","AwayTeam","away_team","Away","away","awayTeamName","AwayTeamName"];for(const k of keys){const n=objName(p?.[k]);if(n)return n}const g=p?.game||p?.Game||p?.match||p?.Match||p?.event||p?.Event;if(g)for(const k of keys){const n=objName(g[k]);if(n)return n}return side==="home"?"Home Team":"Away Team"}
function gameMeta(p){const g=p?.game||p?.Game||p?.match||p?.Match||p?.event||p?.Event||{};const date=val(g,["DateTime","dateTime","Date","date","StartTime","startTime"],null);return date?new Date(date).toLocaleString(undefined,{month:"short",day:"numeric",hour:"numeric",minute:"2-digit"}):null}
function reasons(p){if(Array.isArray(p.reasons)&&p.reasons.length)return p.reasons.slice(0,3).join(" • ");if(typeof p.reasons==="string")return p.reasons;if(p.features&&typeof p.features==="object"){let a=[];for(const [k,v] of Object.entries(p.features)){if(v!=null&&typeof v!=="object")a.push(`${k.replace(/_/g," ")}: ${v}`);if(a.length>=2)break}if(a.length)return a.join(" • ")}return "Signal generated from available performance, form, availability and data-quality factors."}
function render(s="ALL"){let a=s==="ALL"?all:all.filter(p=>String(val(p,["sport","Sport"],"")).toUpperCase()===s);if(!a.length){grid.innerHTML='<div class="empty">No predictions available for this sport right now.</div>';return}grid.innerHTML=a.map(p=>{let c=Number(val(p,["confidence","Confidence"],0));c=c<=1?Math.round(c*100):Math.round(c);let home=team(p,"home"),away=team(p,"away"),pick=val(p,["pick","Pick","prediction","Prediction","selection","Selection"],"Pending");return `<article class="card"><div class="cardtop"><span class="sport">${esc(String(val(p,["sport","Sport"],"SPORT")).toUpperCase())}</span><span class="status">${esc(val(p,["status","Status"],"PENDING"))}</span></div><div class="game">${esc(home)} <span>vs</span> ${esc(away)}</div>${gameMeta(p)?`<div class="meta">${esc(gameMeta(p))}</div>`:""}<div class="pickrow"><strong>${esc(pick)}</strong><span class="conf">${c}% confidence</span></div><div class="bar"><i style="width:${Math.max(0,Math.min(100,c))}%"></i></div><div class="why">${esc(reasons(p))}</div><div class="tags"><span class="tag">V24 PRO INTELLIGENCE</span><span class="tag">EXPLAINABLE</span></div></article>`}).join("")}
async function loadAll(){connection.textContent="Updating live data…";try{const[s,p,d]=await Promise.all([fetch(API+"/api/v24/status").then(r=>r.json()),fetch(API+"/api/v24/predictions").then(r=>r.json()),fetch(API+"/api/v24/dashboard").then(r=>r.json())]);games.textContent=val(s,["games"]);preds.textContent=val(s,["predictions"]);teams.textContent=val(s,["intelligenceTeams","teamsProfiled"]);all=Array.isArray(p)?p:(p.predictions||p.data||p.items||[]);render();record.textContent=`${val(d,["wins"],0)}-${val(d,["losses"],0)}-${val(d,["pushes"],0)}`;wins.textContent=val(d,["wins"],0);losses.textContent=val(d,["losses"],0);pushes.textContent=val(d,["pushes"],0);connection.textContent=`Live • ${all.length} predictions loaded`}catch(e){connection.textContent="Backend unavailable";grid.innerHTML='<div class="empty">The dashboard is online, but the live prediction service is temporarily unavailable.</div>'}}
document.querySelectorAll("#filters button").forEach(b=>b.onclick=()=>{document.querySelectorAll("#filters button").forEach(x=>x.classList.remove("active"));b.classList.add("active");render(b.dataset.sport)});
date.textContent=new Date().toLocaleDateString(undefined,{weekday:"long",month:"long",day:"numeric"});function showPro(){modal.classList.add("show")}function hidePro(){modal.classList.remove("show")}function join(){const e=document.getElementById("email").value;if(e.includes("@"))msg.textContent="You're on the PRO waitlist."}loadAll();